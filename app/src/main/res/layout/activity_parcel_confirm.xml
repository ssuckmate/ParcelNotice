<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="activity"
            type="com.eos.parcelnotice.ParcelConfirmActivity" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".ParcelConfirmActivity">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolBar_parcel"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@color/colorPrimary"
            app:title="택배 확인 "
            app:titleTextColor="#000000"
            android:theme="@style/ToolbarTheme"/>


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/parcel_confirm_recyclerview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="1dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/toolBar_parcel"/>
    </LinearLayout>

</layout>